$(function () {     /**Faz  o efeito a exibi a mensagem*/    $(".notifica").fadeIn(900);    /*Oculta a  mensagem*/    setTimeout("$('.notifica').fadeOut('slow')", 3000);        $(document).on("click", ".aplicar_desconto", function () {        var cupom_desconto = $('#cupom_desconto').val();                if (cupom_desconto) {            $.ajax({                url: '/carrinho/ajax-aplicar-desconto/',                type: 'GET',                data: {                    cupom_desconto: cupom_desconto                },                dataType: 'html'            }).done(function (data) {                if(data && data !=''){                    $('#total_desconto').html(data);                }            });        }    });         $(document).on("click", ".finaliza-carrinho", function () {         alert('A opçao esta em desenvolvimento...');              });});